// Move up

y -= walkspeed;

if (player_direction = -90) {
	image_speed = walkspeed / image_speed_quotent
	sprite_index = sprite_player_running_left
}
if (player_direction = 90) {
	image_speed = walkspeed / image_speed_quotent
	sprite_index = sprite_player_running_right
	}